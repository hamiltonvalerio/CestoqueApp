export declare class CurrencyMask {
    private n;
    private len;
    detectAmountReverse(v: any): string;
    detectAmount(v: any): string;
    private fixAmountReverse(a);
    private fixAmount(a);
    private removeAllDots(a);
}
